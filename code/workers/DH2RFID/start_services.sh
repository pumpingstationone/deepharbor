#!/usr/bin/env bash

# Start the FastAPI development server
# (Change 'dev' to your desired environment if needed)
# Specify the port number as 8001 to avoid conflicts with other services
uv run fastapi dev --port 8001
